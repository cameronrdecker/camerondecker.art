// type = item/itemExternal (for external URLs)
const footerMenuItems = [
    // { type: 'item', label: 'Footer Item', path: '/' },
    // { type: 'itemExternal', label: 'Google', path: 'https://www.google.com/', target: '_BLANK'}
  ]

export default footerMenuItems